# CLAUDE.md - Portfolio 開発ルール

------------------------------------------------------------------------

## 🚨 最重要ルール（絶対遵守）

1.  **PR のマージは絶対に AI（Claude）で実行しない。** マージは必ず人間が手動で行う。
    `gh pr merge` 等の実行はもちろん、提案もしないこと。**PR の作成までは可。**
2.  **このリポジトリは public。** 本名・メールアドレス・勤務先企業名などの個人情報を
    公開ページのコードやコミットに含めてはいけない（後述の「個人情報の取り扱い」参照）。
3.  **`main` への直接コミット・プッシュ禁止。** 必ずフィーチャーブランチで作業する。
4.  **AI による自動 push 禁止。** push は開発者が手動で行う。

------------------------------------------------------------------------

## 🎯 開発方針

本プロジェクトは **AI活用開発** を前提とする。

-   AIを活用するが、**最終責任は開発者が持つ**
-   小さく作り、早く動かし、継続的に改善する
-   完璧を目指さない（まず動くもの）／ 1PR = 1関心事

------------------------------------------------------------------------

## 🚀 技術構成

  | 役割 | 技術 |
  | -- | -- |
  | フレームワーク | Vue 3.4（`<script setup>` / Composition API） |
  | 言語 | TypeScript 5.3 |
  | ビルド | Vite 5 |
  | スタイル | Tailwind CSS 3.4 |
  | 型チェック | vue-tsc（`npm run build` に組み込み） |
  | テスト | Vitest + @vue/test-utils（jsdom 環境） |
  | Lint | ESLint 9（flat config）+ eslint-plugin-vue + typescript-eslint |
  | デプロイ | GitHub Actions → AWS S3 / CloudFront |

------------------------------------------------------------------------

## 🔐 個人情報の取り扱い（重要）

public リポジトリのため、個人情報は公開ページから分離し、暗号化した限定公開ページで表示する。

### 仕組み

-   **公開データ**: `src/data/constants.ts`（サニタイズ済み。名前は "Ume"、メールなし、経歴の企業名なし）
-   **限定公開ページ**: `/#/private` でパスワード入力 → `public/private-data.enc.json` を
    AES-GCM（PBKDF2 210k）で復号して完全版を表示
-   **実データ**: `private-data.json`（**gitignore 対象・ローカルのみ**）。紛失すると再作成が必要
-   **暗号化**: `npm run encrypt -- <パスワード>`（`scripts/encrypt-private-data.mjs`）。
    パスワード変更はこの再実行 → コミット → push のみ

### 関連ファイル

-   `src/data/profile.ts` — 公開データで初期化し、解錠後に完全版で上書きするリアクティブストア
-   `src/data/privateCrypto.ts` — 復号ロジック（PBKDF2 反復回数は暗号化スクリプトと一致させること）
-   `src/components/PrivateGate.vue` — パスワード入力 UI

### ❌ 禁止事項

-   平文の `private-data.json` をコミットすること
-   本名・メール・企業名を `constants.ts` や `index.html` のメタタグに書くこと

### ✅ ルール

-   公開対象に個人情報が含まれていないか、コミット前に必ず確認する
-   暗号化済みの `private-data.enc.json` はリポジトリに含める（配信対象のため）

------------------------------------------------------------------------

## 🧠 AI利用ルール

-   AI生成コードは必ずレビューすること
-   意図を理解できないコードは使用しない
-   不明な処理はコメントで理由を残す
-   AIに丸投げしない（設計・最終判断は人間が行う）

------------------------------------------------------------------------

## 🌿 Git ルール

### ❌ 禁止

-   `main` への直接コミット・プッシュ
-   AI による自動 push
-   **AI による PR マージ**（最重要ルール参照）

### ✅ ルール

-   必ずフィーチャーブランチで作業する

```
git checkout -b feature/add-section
```

-   push は開発者が手動で実行する
-   PR の作成は AI が行ってよいが、マージは人間が行う

------------------------------------------------------------------------

## 🧪 テスト・Lint

-   テストは **Vitest + @vue/test-utils**（jsdom 環境）を使用する
-   テストファイルは対象と同じディレクトリに `*.spec.ts` で配置する
-   ロジック（`src/data/` のストアや復号処理など）と、状態によって描画が変わる
    コンポーネントの挙動を優先的にテストする
-   Lint は **ESLint 9（flat config）**。設定は `eslint.config.js`

### ❌ 禁止事項

-   テストが落ちている / Lint エラーがある状態でコミットすること
-   `npm run build` の型チェック（vue-tsc）が通らない状態でコミットすること

### ✅ コミット前チェック

1.  `npm run test:run` — テストが通る
2.  `npm run lint:check` — Lint エラーがない（`npm run lint` で自動修正）
3.  `npm run build` — 型チェック + ビルドが通る
4.  個人情報まわりの変更時は、公開ページに情報が漏れていないか確認する
5.  UI 変更はブラウザプレビューで実際の表示・挙動を確認する

### コマンド

```bash
npm run dev        # 開発サーバー（http://localhost:5173）
npm run build      # vue-tsc 型チェック + 本番ビルド
npm run preview    # ビルド結果のプレビュー
npm run test       # Vitest（watch モード）
npm run test:run   # Vitest（1回実行・CI 向け）
npm run lint       # ESLint（自動修正あり）
npm run lint:check # ESLint（チェックのみ）
npm run encrypt -- <パスワード>  # private-data.json を暗号化
```

------------------------------------------------------------------------

## 🏷 命名規則

-   変数名は意味が分かる名前にする（不必要な省略禁止）
-   boolean は `is` / `has` を接頭辞に使う
-   関数・メソッドは動詞で開始（create, fetch, update, apply など）
-   単数 / 複数を正確に使い分ける
-   コンポーネントは PascalCase（`HeroSection.vue`）

------------------------------------------------------------------------

## 🧩 実装方針

-   Vue は `<script setup lang="ts">` で記述する
-   表示データは `src/data/` に集約し、コンポーネントから直接ハードコードしない
-   スタイルは Tailwind のユーティリティクラスを基本とする
-   責務ごとにコンポーネント / モジュールを分離する

### ❌ 禁止事項

-   不要なコードを残す（debug / `console.log` など）
-   表示テキストや個人情報をコンポーネントに直接ハードコードする

------------------------------------------------------------------------

## ⚠️ エラーハンドリング

-   例外は握り潰さない（ユーザーに分かるエラー表示か、原因を残す）
-   復号失敗など想定内の失敗は、ユーザー向けメッセージで適切に伝える

------------------------------------------------------------------------

## 📁 ディレクトリ構成

```
src/
├── data/           # 表示データ・プロフィールストア・復号ロジック
├── components/     # セクション単位の Vue コンポーネント
├── directives/     # カスタムディレクティブ
├── App.vue
├── main.ts
└── style.css
public/             # 静的配信ファイル（private-data.enc.json を含む）
scripts/            # 暗号化スクリプトなど
```

------------------------------------------------------------------------

## ⚠️ 既知の注意点

-   過去の git 履歴には本名・メール入りの `constants.ts` が残存している
-   コミット author に個人 Gmail が使われている履歴がある（public で閲覧可能）
